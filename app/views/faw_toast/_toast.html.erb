<% flash.each do |type, message| %>
  <div class="faw-toast <%= flash_class(type) %>" data-controller="toast" data-action="animationend->toast#handleAnimationEnd">
    <%= message %>
    <div class="faw-toast-progress" data-toast-target="progress"></div>
  </div>
<% end %>
